import { Link } from "react-router-dom";

export default function Start(params) {
  return <div>
    <div>Haokai Mao's Website is constructing</div>
    <Link to = "Start">MainPage</Link>
  </div>;
}
